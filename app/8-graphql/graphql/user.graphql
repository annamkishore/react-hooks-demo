type Query {
    getUser(id: ID!): User
    getAllUsers: [User]
}

type User {
    id: ID!
    name: String!
    email: String!
}


type Mutation {
    createUser(name: String!, email: String!): User
}
