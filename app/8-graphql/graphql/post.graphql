type Query {
    getPost(id: ID!): Post
    getAllPosts: [Post]
}

type Post {
    id: ID!
    title: String!
    content: String!
}


type Mutation {
    createPost(title: String!, content: String!): Post
}
